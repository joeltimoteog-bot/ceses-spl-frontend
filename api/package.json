{
  "name": "ceses-spl-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create ceses-spl",
    "db:schema": "wrangler d1 execute ceses-spl --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute ceses-spl --local --file=./schema.sql"
  },
  "devDependencies": { "wrangler": "^4" }
}
